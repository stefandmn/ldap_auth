{
  "config": {
    "abort": {
      "single_instance_allowed": "Only one LDAP Integration configuration is allowed."
    },    
    "step": {
      "user": {
        "title": "LDAP Integration",
        "description": "Configure LDAP connection parameters used by the command_line authentication script.",
        "data": {
          "server": "LDAP server URI (ldap:// or ldaps://)",
          "helperdn": "Service bind DN",
          "helperpass": "Service password",
          "basedn": "Base DN for user search",
          "attrs": "Username attribute (e.g. uid, sAMAccountName)",
          "base_filter": "Base LDAP filter (combined with username filter)",
          "display_attr": "Display name attribute (meta mode)",
          "timeout": "Timeout (seconds)",
          "verify_ssl": "Verify TLS certificates",
          "use_starttls": "Use StartTLS (ldap:// only)"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to LDAP server",
      "invalid_auth": "Invalid user credentials or binding DN/password",
      "invalid_config": "Invalid configuration, missing attributes aor parameters",
      "invalid_operation": "Invalid LDAP operation"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "LDAP Integration",
        "description": "Edit LDAP connection parameters used by the command_line authentication script.",
        "data": {
          "server": "LDAP server URI (ldap:// or ldaps://)",
          "helperdn": "Service bind DN",
          "helperpass": "Service password",
          "basedn": "Base DN for user search",
          "attrs": "Username attribute",
          "base_filter": "Base LDAP filter",
          "display_attr": "Display name attribute",
          "timeout": "Timeout (seconds)",
          "verify_ssl": "Verify TLS certificates",
          "use_starttls": "Use StartTLS (ldap:// only)"
        }
      }
    }
  }
}